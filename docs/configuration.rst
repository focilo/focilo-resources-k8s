Configuration
=============


REANA-Resources-K8 default congifuration is defined in `templates/config.yaml` file. It is printed below for inspiration. You can define your own `config.yaml` and then use `reana-resources-k8 build-manifests` to fit your computing cloud.

.. literalinclude:: ../reana_resources_k8s/templates/config.yaml
    :language: yaml
